# 第七章：卷积

《Programming Massively Parallel Processors》第四版 - 学习笔记与练习

## 📚 学习内容

本章系统梳理卷积运算及其 CUDA 优化技术：

- 1D/2D/3D 卷积运算原理
- Ghost cells（边界处理）
- 常量内存（Constant Memory）
- Tiled 卷积与共享内存
- L2 缓存利用

**相关博客笔记**：[第七章：卷积](https://smarter.xin/posts/1c778456/)

---

## 💻 代码实现

### Exercise01 - 2D卷积基础

实现朴素2D卷积和使用常量内存的2D卷积（对应图7.7和图7.9）。

**代码位置**：`Exercise01/`

**核心实现**：

| Kernel | 特点 |
| ------ | ---- |
| `conv2d_basic_kernel` | 朴素实现，滤波器从全局内存读取 |
| `conv2d_const_memory_kernel` | 滤波器存储在常量内存，缓存广播 |

```cuda
// 常量内存版本核心代码
__constant__ float d_F[MAX_FILTER_SIZE];

__global__ void conv2d_const_memory_kernel(float* N, float* P, int r, int height, int width) {
    int outRow = blockIdx.y * blockDim.y + threadIdx.y;
    int outCol = blockIdx.x * blockDim.x + threadIdx.x;
    
    if (outRow < height && outCol < width) {
        float Pvalue = 0.0f;
        for (int fRow = 0; fRow < 2*r+1; ++fRow) {
            for (int fCol = 0; fCol < 2*r+1; ++fCol) {
                int inRow = outRow - r + fRow;
                int inCol = outCol - r + fCol;
                if (inRow >= 0 && inRow < height && inCol >= 0 && inCol < width) {
                    Pvalue += N[inRow * width + inCol] * d_F[fRow * (2*r+1) + fCol];
                }
            }
        }
        P[outRow * width + outCol] = Pvalue;
    }
}
```

#### 运行 Exercise01

```bash
cd Exercise01
make
make run
```

#### 预期输出

```text
================================================
  第七章：卷积
  2D Convolution - Basic & Constant Memory
  参考: chapter-07/code/conv2d_kernels.cu
================================================

矩阵大小: 64 × 64
滤波器大小: 3 × 3 (半径=1)

高斯滤波器:
0.0751  0.1238  0.0751
0.1238  0.2042  0.1238
0.0751  0.1238  0.0751

=== CPU 参考实现 ===
完成

=== 朴素 GPU 实现 ===
✅ 结果正确！

=== 常量内存 GPU 实现 ===
✅ 结果正确！

================================================
  小矩阵演示
================================================

输入矩阵 (4×4 全1):
1.0000  1.0000  1.0000  1.0000
1.0000  1.0000  1.0000  1.0000
1.0000  1.0000  1.0000  1.0000
1.0000  1.0000  1.0000  1.0000

高斯滤波器 (3×3):
0.0625  0.1250  0.0625
0.1250  0.2500  0.1250
0.0625  0.1250  0.0625

卷积结果:
0.6250  0.7500  0.7500  0.6250
0.7500  1.0000  1.0000  0.7500
0.7500  1.0000  1.0000  0.7500
0.6250  0.7500  0.7500  0.6250

【关键概念】
------------------------------------------------
• 朴素卷积：滤波器从全局内存读取
• 常量内存：滤波器缓存在常量内存中
• 常量内存优势：所有线程访问相同位置时广播
• Ghost cells：边界外的元素视为 0

✅ 测试完成！
```

---

### Exercise02 - Tiled 2D卷积

实现 Tiled 2D卷积，使用共享内存优化（对应图7.12和图7.15）。

**代码位置**：`Exercise02/`

**核心实现**：

| Kernel | 特点 |
| ------ | ---- |
| `conv2d_tiled_kernel` | 完整 input tile 加载到共享内存 |
| `conv2d_tiled_cached_kernel` | 输出 tile 用共享内存，边界依赖L2缓存 |

```cuda
// Tiled 卷积核心代码
#define IN_TILE_SIZE 32
#define OUT_TILE_SIZE (IN_TILE_SIZE - 2 * FILTER_RADIUS)

__global__ void conv2d_tiled_kernel(float* N, float* P, int r, int height, int width) {
    __shared__ float N_s[IN_TILE_SIZE][IN_TILE_SIZE];
    
    int row = blockIdx.y * OUT_TILE_SIZE + threadIdx.y - FILTER_RADIUS;
    int col = blockIdx.x * OUT_TILE_SIZE + threadIdx.x - FILTER_RADIUS;
    
    // 协作加载
    if (row >= 0 && row < height && col >= 0 && col < width) {
        N_s[threadIdx.y][threadIdx.x] = N[row * width + col];
    } else {
        N_s[threadIdx.y][threadIdx.x] = 0.0f;
    }
    __syncthreads();
    
    // 只有输出 tile 内的线程计算
    int tileRow = threadIdx.y - FILTER_RADIUS;
    int tileCol = threadIdx.x - FILTER_RADIUS;
    if (tileRow >= 0 && tileRow < OUT_TILE_SIZE && 
        tileCol >= 0 && tileCol < OUT_TILE_SIZE) {
        // 从共享内存读取计算
    }
}
```

#### 运行 Exercise02

```bash
cd Exercise02
make
make run
```

#### 预期输出

```text
================================================
  第七章：卷积
  Tiled 2D Convolution
  参考: chapter-07/code (Fig 7.12, 7.15)
================================================

矩阵大小: 128 × 128
滤波器大小: 3 × 3 (半径=1)
IN_TILE_SIZE: 32, OUT_TILE_SIZE: 30

高斯滤波器:
0.0751  0.1238  0.0751
0.1238  0.2042  0.1238
0.0751  0.1238  0.0751

=== CPU 参考实现 ===
完成

=== Tiled GPU 实现 (图7.12) ===
✅ 结果正确！

=== Tiled + L2缓存 GPU 实现 (图7.15) ===
✅ 结果正确！

【关键概念】
------------------------------------------------
• Tiled 卷积：共享内存存储整个输入 tile（含 halo）
• 输入 tile 大小: 32 × 32
• 输出 tile 大小: 30 × 30
• L2 缓存版本：边界元素依赖 L2 缓存命中
  - 共享内存使用减少
  - 适合小滤波器场景

✅ 测试完成！
```

---

### Exercise03 - 3D卷积（练习8-10）

实现练习8、9、10要求的3D卷积 kernel。

**代码位置**：`Exercise03/`

| 练习 | 实现 | 说明 |
| ---- | ---- | ---- |
| 练习8 | `conv3d_basic_kernel` | 图7.7扩展到3D |
| 练习9 | `conv3d_const_memory_kernel` | 图7.9扩展到3D |
| 练习10 | `conv3d_tiled_kernel` | 图7.12扩展到3D |

#### 运行 Exercise03

```bash
cd Exercise03
make
make run
```

#### 预期输出

```text
================================================
  第七章：卷积
  3D Convolution (练习 8-10)
  参考: chapter-07 README.md
================================================

体积大小: 16 × 16 × 16
滤波器大小: 3 × 3 × 3 (半径=1)
IN_TILE_DIM: 8, OUT_TILE_DIM: 6

3D 高斯滤波器 (中心切片 z=1):
0.0370  0.0444  0.0370
0.0444  0.0533  0.0444
0.0370  0.0444  0.0370

=== CPU 参考实现 ===
完成

=== 练习 8：基础 3D 卷积 ===
✅ 结果正确！

=== 练习 9：常量内存 3D 卷积 ===
✅ 结果正确！

=== 练习 10：Tiled 3D 卷积 ===
✅ 结果正确！

================================================
  输出对比 (中心切片 z=8)
================================================

CPU 结果:
0.2345  0.3456  0.4567  ...
0.3456  0.4567  0.5678  ...
...

【关键概念】
------------------------------------------------
• 3D 卷积：扩展2D卷积到三维空间
• 滤波器大小：(2r+1)³ = 27 个元素
• 计算复杂度：每个输出需要 27 次乘加
• Block 设计：使用较小的 block (8³=512 线程)
• 共享内存：3D tile 需要更多内存

✅ 测试完成！
```

---

## 📖 练习题解答

### 练习 1

**题目：** 计算图7.3中的 P[0] 值。

**解答：**

```
x = [0, 0, 8, 2, 5]
f = [1, 3, 5, 3, 1]
P[0] = 0×1 + 0×3 + 8×5 + 2×3 + 5×1 = 0 + 0 + 40 + 6 + 5 = 51
```

---

### 练习 2

**题目：** 对数组 N = {4,1,3,2,3} 使用滤波器 F = {2,1,4} 进行1D卷积，求输出数组。

**解答：**

添加 ghost cells：`N_padded = [0, 4, 1, 3, 2, 3, 0]`

```
P[0] = [0, 4, 1] · [2, 1, 4] = 0 + 4 + 4 = 8
P[1] = [4, 1, 3] · [2, 1, 4] = 8 + 1 + 12 = 21
P[2] = [1, 3, 2] · [2, 1, 4] = 2 + 3 + 8 = 13
P[3] = [3, 2, 3] · [2, 1, 4] = 6 + 2 + 12 = 20
P[4] = [2, 3, 0] · [2, 1, 4] = 4 + 3 + 0 = 7

P = [8, 21, 13, 20, 7]
```

---

### 练习 3

**题目：** 以下1D卷积滤波器的作用是什么？

**a. [0 1 0]**

**解答：** 恒等滤波器。输出等于输入。

**b. [0 0 1]**

**解答：** 右移滤波器。将信号向右移动一个位置。

**c. [1 0 0]**

**解答：** 左移滤波器。将信号向左移动一个位置。

**d. [-1/2 0 1/2]**

**解答：** 边缘检测（差分）滤波器。检测信号的变化率，相邻值相似则输出趋近于0。

**e. [1/3 1/3 1/3]**

**解答：** 均值滤波器。对信号进行平滑处理，取相邻三个值的平均。

---

### 练习 4

**题目：** 对大小为 N 的数组使用大小为 M 的滤波器进行1D卷积：

**a. 总共有多少 ghost cells？**

**解答：** 滤波器半径 `r = (M-1)/2`，左右各有 r 个 ghost cells，总共 **M-1** 个。

**b. 如果 ghost cells 也计为乘法（乘以0），总共有多少次乘法？**

**解答：** 每个输出元素需要 M 次乘法，共 N 个输出，总共 **N × M** 次乘法。

**c. 如果 ghost cells 不计为乘法，总共有多少次乘法？**

**解答：**

- 完整乘法：N × M
- 边界处减少的乘法：`r + (r-1) + ... + 1 = r(r+1)/2`（左右各一次）
- 总计：**N × M - r(r+1)** 次乘法

---

### 练习 5

**题目：** 对 N × N 矩阵使用 M × M 滤波器进行2D卷积：

**a. 总共有多少 ghost cells？**

**解答：**

- 四条边：`4 × N × r`
- 四个角：`4 × r²`
- 总计：**4r × (N + r)** 个 ghost cells

**b. 如果 ghost cells 也计为乘法，总共有多少次乘法？**

**解答：** **N² × M²** 次乘法

**c. 如果 ghost cells 不计为乘法，总共有多少次乘法？**

**解答：**

```
N² × M² - 2 × N × r × (r+1) - 4 × [r(r+1)/2]²
```

---

### 练习 6

**题目：** 对 N₁ × N₂ 矩阵使用 M₁ × M₂ 滤波器进行2D卷积：

设 `r₁ = (M₁-1)/2`，`r₂ = (M₂-1)/2`

**a. 总共有多少 ghost cells？**

**解答：** **2 × (N₁ × r₂ + N₂ × r₁) + 4 × r₁ × r₂**

**b. 如果 ghost cells 也计为乘法，总共有多少次乘法？**

**解答：** **N₁ × N₂ × M₁ × M₂**

**c. 如果 ghost cells 不计为乘法，总共有多少次乘法？**

**解答：**

```
N₁ × N₂ × M₁ × M₂ - N₁ × r₂ × (r₂+1) - N₂ × r₁ × (r₁+1) - [r₁(r₁+1)/2] × [r₂(r₂+1)/2]
```

---

### 练习 7

**题目：** 考虑使用图7.12的 kernel 对 N × N 数组进行 Tiled 卷积，滤波器 M × M，输出 tile T × T：

**a. 需要多少个线程块？**

**解答：** `[(N + T - 1)/T] × [(N + T - 1)/T]` 个线程块

**b. 每个块需要多少线程？**

**解答：** 输入 tile 大小为 `T + M - 1`，每块 **(T + M - 1)² 个线程**

**c. 每个块需要多少共享内存？**

**解答：** **(T + M - 1)² × 4 字节**（float类型）

**d. 如果使用图7.15的 kernel，上述问题的答案是什么？**

**解答：**

- 线程块数：相同，`[(N + T - 1)/T]²`
- 每块线程数：**T² 个线程**
- 共享内存：**T² × 4 字节**

---

### 练习 8

**题目：** 将图7.7的2D kernel 修改为3D卷积。

**解答：** 见 `Exercise03/solution.cu` 中的 `conv3d_basic_kernel` 实现。

关键修改：

- 添加 z 维度的循环和坐标计算
- 3D 索引：`index = z * width * height + y * width + x`

---

### 练习 9

**题目：** 将图7.9的2D kernel 修改为3D卷积。

**解答：** 见 `Exercise03/solution.cu` 中的 `conv3d_const_memory_kernel` 实现。

关键修改：

- 使用3D常量内存数组
- 3D 滤波器索引计算

---

### 练习 10

**题目：** 将图7.12的 Tiled 2D kernel 修改为3D卷积。

**解答：** 见 `Exercise03/solution.cu` 中的 `conv3d_tiled_kernel` 实现。

关键修改：

- 3D 共享内存：`__shared__ float N_s[DIM][DIM][DIM]`
- 3D 线程块和网格配置
- 注意：3D 共享内存需求较大，需要减小 tile 尺寸

---

## 🔧 开发环境

- **CUDA Toolkit**: 11.0 或更高版本
- **编译器**: GCC 7.5+ / Visual Studio 2019+ + NVCC
- **GPU**: 支持 CUDA 的 NVIDIA 显卡（计算能力 3.5+）

## 💡 学习建议

1. **理解卷积运算**：先手算小例子理解原理
2. **边界处理**：Ghost cells 的概念很重要
3. **常量内存**：适合所有线程访问相同数据的场景
4. **Tiled 设计**：权衡输入输出 tile 大小关系
5. **3D 扩展**：注意共享内存和线程数限制

## 🚀 下一步

完成本章学习后，继续学习：

- 第八章：模板
- 第九章：并行直方图
- 第十章：归约

---

## 📚 参考资料

- PMPP 第四版 Chapter 7
- [第七章：卷积](https://smarter.xin/posts/1c778456/)

**学习愉快！** 🎓
